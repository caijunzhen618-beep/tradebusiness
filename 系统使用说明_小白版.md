# 货运代理业务管理系统使用说明（小白版）

> 适用版本：TradeBusiness v1.0  
> 本文面向第一次使用系统的管理员和业务员，按“启动 → 登录 → 日常操作 → 常见问题”的顺序编写。

## 一、先弄清楚三个页面

系统由三个服务组成：

| 名称 | 地址 | 谁使用 | 主要用途 |
| --- | --- | --- | --- |
| 管理端 | http://localhost:3000 | 管理员 | 管理用户、客户、采集、AI、邮件、积分和系统配置 |
| 客户端 | http://localhost:3001 | 管理员或业务员 | 日常跟进客户、发邮件、处理任务和查看数据 |
| 后端接口 | http://localhost:8000 | 系统内部 | 提供登录、数据保存、邮件、AI 和自动化服务 |

简单理解：管理端负责“管全局”，客户端负责“做业务”。两端操作的是同一套数据。

## 二、第一次启动系统（Windows）

### 2.1 启动前准备

电脑需要安装：

- Python 3.11 或更高版本
- Node.js 18 或更高版本
- MySQL 8.0 或更高版本
- Redis（自动化任务、消息和后台任务需要）

如果项目已经由技术人员安装好依赖，直接执行下一步即可。

Ubuntu 第一次使用脚本时，先执行一次：

```bash
cd /path/to/tradebusiness
chmod +x start-all.sh stop-all.sh
```

### 2.2 一键启动

1. 打开项目目录：`C:\commonspace\ai_create\tradebusiness`
2. 双击 `start-all.bat`
3. 不要关闭启动后弹出的黑色命令窗口
4. 等待看到后端、管理端、客户端分别启动
5. 浏览器打开：
   - 管理端：http://localhost:3000
   - 客户端：http://localhost:3001

如果依赖文件已经变化，需要强制重新安装依赖：

```bash
INSTALL_DEPS=1 ./start-all.sh
```

停止系统时，双击项目根目录的 `stop-all.bat`。

### 2.3 第一次启动失败怎么办

按以下顺序检查：

1. MySQL 是否正在运行。
2. `tradebusiness-backend\.env` 中的数据库用户名、密码和数据库名是否正确。
3. 端口 3000、3001、8000 是否被其他软件占用。
4. 如果提示依赖缺失，在对应目录执行：

```powershell
cd tradebusiness-backend
venv\Scripts\activate
pip install -r requirements.txt

cd ..\tradebusiness-admin
npm install

cd ..\tradebusiness-client
npm install
```

5. 如果数据库还没有初始化，在后端目录执行：

```powershell
cd tradebusiness-backend
venv\Scripts\activate
python scripts\init_db.py
```

更多启动问题请查看 [QUICKSTART.md](./QUICKSTART.md)、[STARTUP_VERIFICATION.md](./STARTUP_VERIFICATION.md) 和 [FAQ.md](./FAQ.md)。

## 三、登录系统

### 3.1 默认账号

| 账号 | 密码 | 权限 |
| --- | --- | --- |
| `admin` | `Admin123` | 管理员，可使用管理端全部功能 |
| `sales01` | `Sales123` | 业务员，主要使用客户端处理自己的业务 |

首次登录后，建议立即修改默认密码，不要在生产环境继续使用默认密码。

### 3.2 登录步骤

1. 打开管理端或客户端地址。
2. 输入用户名和密码。
3. 点击“登录”。
4. 登录成功后，浏览器会记住登录状态。
5. 如果页面提示登录过期，重新登录即可。

管理员可以在“用户管理”中创建业务员账号、修改用户状态和重置密码。

## 四、管理员使用说明

管理员建议按以下顺序配置系统：

1. 系统设置
2. 用户管理
3. 客户管理
4. 邮件配置
5. 销售智能体和 AI 获客配置
6. 自动化任务
7. 数据统计

### 4.1 仪表盘

入口：管理端 → 仪表盘。

这里查看系统整体情况，例如客户数量、任务数量、邮件情况、近期业务趋势和待处理事项。仪表盘适合每天开工时查看，不建议直接在这里修改基础数据。

### 4.2 用户管理

入口：管理端 → 用户管理。

常用操作：

- 新增业务员：点击“新增”，填写用户名、姓名、邮箱和初始密码。
- 修改用户：点击列表中的编辑按钮。
- 启用或停用账号：关闭账号后，该用户不能继续登录。
- 设置角色：管理员拥有全局权限，业务员通常只处理分配给自己的客户和任务。

建议每位员工使用独立账号，不要多人共用 `sales01`。

### 4.3 客户管理

入口：管理端 → 客户管理。

这是客户资料的总库。可以完成：

- 新增客户：录入公司名称、国家、城市、联系人、邮箱、电话和备注。
- 搜索客户：按公司名、联系人、邮箱等关键词搜索。
- 筛选客户：按国家、状态、优先级和负责人筛选。
- 分配客户：将客户分配给指定业务员。
- 编辑客户：修正客户资料、状态和备注。
- 导入客户：使用批量导入功能上传 Excel 或 CSV 文件。
- 导出数据：导出当前筛选结果进行备份或线下分析。

批量导入前建议先准备少量测试数据，确认字段对应无误后再导入大批量文件。邮箱、公司名称和国家等关键字段尽量填写完整。

### 4.4 任务管理

入口：管理端 → 任务管理。

管理员可以查看所有任务、创建任务、分配负责人、设置优先级和截止时间，并查看任务是否已完成或逾期。

建议任务标题写清楚“对谁做什么”，例如：`跟进 ABC Logistics 的报价反馈`，不要只写“跟进客户”。

### 4.5 通知中心

入口：管理端 → 通知中心。

查看新任务、邮件结果、自动化任务和系统提醒。未读通知通常会显示在顶部铃铛图标上。打开通知后可按页面提供的操作标记为已读。

### 4.6 AI 获客

入口：管理端 → AI 获客。

典型流程：

1. 填写目标国家、城市、行业、关键词和需要采集的数量。
2. 点击开始生成或搜索。
3. 等待任务完成。
4. 在结果中检查公司名称、网站、邮箱和电话。
5. 删除明显错误或重复数据。
6. 将合格线索导入客户库。

AI 生成的内容必须人工检查，尤其是邮箱、公司是否真实以及是否允许联系。不要把未经核验的线索直接批量发送邮件。

### 4.7 数据采集

入口：管理端 → 数据采集。

用于执行网站或搜索数据采集任务。填写关键词、国家、地区和数量后启动任务，在任务列表中查看运行状态，再到“采集线索”中检查结果。

采集速度不要设置过快，避免目标网站限制访问。采集结果需要去重和人工确认后再进入客户库。

### 4.8 销售智能体和话术模板

入口：管理端 → 销售智能体、话术模板。

销售智能体用于定义 AI 的工作角色和写作风格，例如“非洲航线开发顾问”。话术模板用于保存可重复使用的开发信提示词。

建议一个模板只服务一个场景，例如：首次开发、报价跟进、沉默客户唤醒。模板中不要写入银行卡密码、API 密钥等敏感信息。

### 4.9 跟进任务

入口：管理端 → 跟进任务。

查看 AI 或人工创建的跟进事项，复制建议话术，记录联系结果，并根据实际反馈调整下次跟进时间。

如果客户明确表示不希望继续联系，应标记为“退订/不联系”，后续自动跟进会跳过该客户。

### 4.10 邮件管理

入口：管理端 → 邮件管理。

可以查看邮件记录、发送状态、失败原因、打开和点击统计。发送邮件前请先在系统设置中配置 SMTP。

SMTP 配置示例：

```env
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=邮箱授权码
SMTP_FROM=your-email@example.com
SMTP_FROM_NAME=TradeBusiness
```

建议先给自己的邮箱发送测试邮件，确认发件人、标题、正文和回复地址正确后，再进行批量发送。

### 4.11 合作管理

入口：管理端 → 合作管理。

用于记录合作伙伴、渠道或外部协作信息。填写合作方基本信息、联系人、合作状态和备注，方便后续统一查询。

### 4.12 数据统计

入口：管理端 → 数据统计。

查看客户来源、客户状态、业务员表现、邮件效果和转化趋势。统计结果依赖已录入的数据，数据越完整，图表越有参考价值。

### 4.13 积分套餐和积分钱包

入口：管理端 → 积分套餐、积分钱包。

AI 获客、背调和内容生成可能消耗积分。管理员可以设置套餐、查看余额、查看充值订单和积分流水。调整积分时必须填写清晰说明，便于审计。

支付平台的配置和上线步骤请查看 [PAYMENT_INTEGRATION_SIMPLE.md](./PAYMENT_INTEGRATION_SIMPLE.md)。

### 4.14 系统设置

入口：管理端 → 系统设置。

可配置个人资料、安全设置、通知偏好、SMTP 和系统参数。生产环境修改配置后，通常需要重启后端服务才能生效。

## 五、业务员客户端使用说明

客户端适合每天实际做业务。推荐工作顺序是：

1. 首页查看今天的任务和提醒。
2. 查看“我的客户”。
3. 在“发现客户”中寻找新客户。
4. 发送邮件或记录沟通。
5. 创建下一次跟进任务。
6. 在消息中心处理新通知。
7. 通过数据分析查看效果。

### 5.1 首页

入口：客户端 → 首页。

查看个人客户数量、待办任务、近期邮件和需要优先处理的客户。每天开始工作时先看首页，可以避免遗漏逾期事项。

### 5.2 我的客户

入口：客户端 → 我的客户。

这里只显示当前业务员负责的客户。可以搜索、筛选、打开详情、修改客户资料、记录备注和更新客户状态。

建议每次电话、邮件或 WhatsApp 沟通后，都在客户备注或跟进记录中写下结果，例如“已报价，客户周五回复”。

### 5.3 未分配客户

入口：客户端 → 未分配客户。

查看尚未分配负责人的客户。业务员可以根据权限领取或申请处理，领取后客户会进入“我的客户”。如果看不到领取按钮，说明当前账号没有该权限，需要联系管理员分配。

### 5.4 发现客户

入口：客户端 → 发现客户。

按国家、城市、行业和关键词寻找潜在客户。发现结果需要先检查真实性，再保存或提交到客户库。不要重复保存同一家公司的多个相同记录。

### 5.5 邮件

入口：客户端 → 邮件。

可以查看收件箱、发送记录和邮件状态。写邮件时：

1. 选择或填写收件人。
2. 填写明确的主题。
3. 正文尽量针对客户行业和路线定制。
4. 检查附件和签名。
5. 发送后到邮件记录查看是否成功。

需要新建邮件时使用“写邮件”。如果发送失败，先检查 SMTP 配置、收件人地址和后端日志。

### 5.6 消息中心

入口：客户端 → 消息中心。

查看任务提醒、邮件结果和系统推送。重要消息处理完后及时标记已读。

### 5.7 任务管理

入口：客户端 → 任务管理。

查看分配给自己的任务，按待处理、进行中、已完成和逾期筛选。完成动作后点击完成并填写结果，避免只完成状态而没有业务记录。

### 5.8 数据分析

入口：客户端 → 数据分析。

查看个人客户、邮件发送、回复和转化情况。可用它判断哪些国家、行业和邮件主题更有效。

### 5.9 AI 获客

入口：客户端 → AI 获客。

业务员可以使用管理员开放的 AI 能力生成潜客、企业背调和开发信内容。AI 生成内容只作为建议，发送前必须检查事实、联系人称呼、公司名称和联系方式。

### 5.10 自动化

入口：客户端 → 自动化。

查看节假日问候、自动跟进和提醒规则。自动化开启前应先确认：

- 收件人资料准确；
- 客户没有标记为不联系；
- 邮件模板内容正确；
- 发送时间和时区正确；
- SMTP 已测试成功。

不确定时先关闭自动发送，使用手动审核模式。

### 5.11 个人中心和系统设置

入口：客户端 → 个人中心、系统设置。

修改姓名、邮箱、密码和通知偏好。修改密码后需要重新登录。业务员不能修改管理员级别的全局配置。

## 六、推荐的完整业务流程

### 6.1 从新线索到成交

```text
AI 获客/数据采集
      ↓
人工检查和去重
      ↓
保存到客户库并分配业务员
      ↓
发送首次开发信
      ↓
创建跟进任务
      ↓
记录回复、报价和沟通结果
      ↓
更新客户状态和下一步计划
```

### 6.2 每日工作清单

1. 登录客户端查看首页。
2. 先处理逾期和高优先级任务。
3. 查看新消息和邮件回复。
4. 更新昨天联系过的客户记录。
5. 给有明确下一步的客户创建任务。
6. 下班前确认没有未处理的高优先级任务。

## 七、数据和安全注意事项

- 不要多人共用账号。
- 不要把密码、SMTP 授权码、支付密钥发到群聊或写进代码。
- 生产环境必须修改默认管理员密码。
- 批量邮件前先小范围测试，遵守客户退订要求。
- 客户明确拒绝联系后，标记为“不联系”，不要再次自动触达。
- AI 和采集结果必须人工核验。
- 重要客户数据定期备份，备份文档参见 [PRODUCTION_DEPLOYMENT.md](./PRODUCTION_DEPLOYMENT.md)。

## 八、常见问题

### 8.1 页面打不开

确认 `start-all.bat` 是否仍在运行，并分别检查 3000、3001、8000 端口。若只需要启动某一端，可使用项目中的 `start-backend.bat`、`start-admin.bat` 或 `start-client.bat`。

### 8.2 登录时报 CORS 错误

确认访问地址和配置一致：推荐统一使用 `http://localhost:3000` 与 `http://localhost:3001`，不要在同一次操作中混用 `localhost` 和 `127.0.0.1`。修改后重启后端。

### 8.3 登录后马上掉线

检查后端是否正常运行、系统时间是否正确，以及浏览器是否禁用了本地存储。清理浏览器缓存后重新登录。

### 8.4 邮件发送失败

按顺序检查 SMTP 主机、端口、账号、授权码、发件人地址和网络连接。Gmail、QQ、163 等邮箱通常需要使用“授权码”，不能直接使用网页登录密码。

### 8.5 AI 没有结果或任务一直等待

检查 AI API 配置、网络连接、积分余额和后台任务服务。先用少量数据测试，不要一次提交很大的采集数量。

### 8.6 自动化没有执行

确认 Redis、Celery Worker 和 Celery Beat 已启动，规则处于启用状态，执行时间和时区正确，并检查客户是否被标记为不联系。

### 8.7 顶部菜单显示不完整

刷新页面或重启前端开发服务。当前导航已支持超出折叠；窗口较窄时，部分菜单会进入“更多”菜单。

## 九、建议的上手练习

第一次使用时不要直接导入全部真实数据，可以按下面的小练习验证系统：

1. 使用 `admin` 登录管理端。
2. 新增一个测试业务员。
3. 新增一家测试客户并分配给该业务员。
4. 使用业务员账号登录客户端。
5. 查看“我的客户”。
6. 创建一条跟进任务。
7. 配置 SMTP 后给自己的邮箱发送测试邮件。
8. 在管理端查看任务、邮件和统计数据。
9. 确认无误后，再导入真实客户和启用自动化。

## 十、给管理员的上线前检查

- [ ] 已修改默认管理员密码
- [ ] 已创建正式业务员账号
- [ ] 已配置并测试 MySQL
- [ ] 已配置 SMTP 并发送测试邮件
- [ ] 已配置 AI API 和积分规则
- [ ] 已确认 Redis、Celery Worker、Celery Beat 正常
- [ ] 已测试客户导入、分配、邮件和任务流程
- [ ] 已设置备份方案
- [ ] 已告知员工退订和客户数据使用规则

支付平台的傻瓜式配置请继续参考：[PAYMENT_INTEGRATION_SIMPLE.md](./PAYMENT_INTEGRATION_SIMPLE.md)。
